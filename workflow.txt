Start by clearing the client files in the api folder, this can be included in the script of building where you mv -f in the build script.

After that everything checks out just ensure to get the directories right. Everything else checks out.